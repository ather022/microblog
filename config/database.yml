default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  #postgresapp.com/documentation/configuration-ruby.html
  adapter: postgresql
  database:     #no db for localhost:3000
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: # Do not set this db to the same as development or production.
  pool: 5
  timeout: 5000

production:
  adapter: postgresql
  database: df72rbvos7vf2b
  pool: 5
  timeout: 5000
